<configuration>
    <!-- Console appender for general logs -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Appender for login-specific logs -->
    <appender name="LOGIN_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>/app/logs/login.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>/app/logs/login-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Logger only for UserService -->
    <logger name="com.reloaded.com.reloaded.sales.service.UserService" level="INFO" additivity="false">
        <appender-ref ref="LOGIN_FILE"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- Root logger for everything else -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
